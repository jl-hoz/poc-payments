{
  "name": "stripe server nodejs",
  "version": "1.0.0",
  "description": "Stripe accept payment server with nodejs",
  "main": "server.js",
  "scripts": {
    "build": "babel server.js -d dist",
    "start": "nodemon --exec babel-node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "José Luis de la Hoz",
  "license": "UNLICENSED",
  "dependencies": {
    "body-parser": "^1.19.1",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "stripe": "^8.202.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.3",
    "@babel/core": "^7.17.5",
    "@babel/node": "^7.16.8",
    "@babel/preset-env": "^7.16.11",
    "nodemon": "^2.0.15"
  }
}
